source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in black_sky.gemspec
gemspec

gem 'rack-cors'
gem 'thin'
gem 'sinatra'
